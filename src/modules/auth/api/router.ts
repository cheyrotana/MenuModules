// TODO: Implement auth routes
// Example: POST /v1/auth/login, POST /v1/auth/register, POST /v1/auth/refresh
import { Router } from "express";

export const authRouter = Router();

// authRouter.post('/v1/auth/login', ...);
// authRouter.post('/v1/auth/register', ...);
// authRouter.post('/v1/auth/refresh', ...);
// authRouter.post('/v1/auth/logout', ...);
